1. In Codex sidebar:
    When asked for "something" (action, question, etc.);

        if "something" better manually (command line, installation or configuration, etc.)

        - propose guide step by step to do it manually

        if not (can be full "something" or just a/some part(s) of it) do it automatically by Codex